---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

<section class="header-masthead">
  <div class="content-780 flow">
    <h1>{{ metadata.title }}</h1>
    <p>Edit <code>src/_data/metadata.json</code> with your blog’s information. Remove/edit this text in <code>src/index.njk</code>.</p>
  </div>
</section>


<section class="content-780">
  {% set maxPosts = collections.posts.length | min(3) %}
  <h2>Latest {% if maxPosts == 1 %}Post{% else %}{{ maxPosts }} Posts{% endif %}</h2>

  {% set postslist = collections.posts | head(-3) %}
  {% set postslistCounter = collections.posts | length %}
  {% include "postslist.njk" %}

  <p>More posts can be found in <a href="{{ '/posts/' | url }}">the archive</a>.</p>

</section>
